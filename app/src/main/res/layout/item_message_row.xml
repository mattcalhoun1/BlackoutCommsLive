<?xml version="1.0" encoding="utf-8"?>
<!-- Used by ui/messages/MessageAdapter for the Messages view -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    android:paddingTop="10dp"
    android:paddingBottom="6dp">

    <!-- Top row: message type icon + title + timestamp -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="5dp">

        <!-- Message type icon (sent/received, direct/broadcast) -->
        <ImageView
            android:id="@+id/img_msg_type"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_marginEnd="8dp"
            android:contentDescription="Message type" />

        <TextView
            android:id="@+id/tv_msg_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary" />

        <TextView
            android:id="@+id/tv_msg_timestamp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="10sp"
            android:fontFamily="monospace"
            android:textColor="@color/text_secondary"
            android:layout_marginStart="8dp" />

    </LinearLayout>

    <!-- From row: device icon + device name -->
    <LinearLayout
        android:id="@+id/row_msg_from"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="3dp">

        <ImageView
            android:id="@+id/img_msg_from_device"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginEnd="6dp"
            android:alpha="0.75"
            android:contentDescription="Sender device type" />

        <TextView
            android:id="@+id/tv_msg_from"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:fontFamily="monospace"
            android:textColor="@color/text_secondary" />

    </LinearLayout>

    <!-- To row: device icon + device name -->
    <LinearLayout
        android:id="@+id/row_msg_to"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="5dp">

        <ImageView
            android:id="@+id/img_msg_to_device"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginEnd="6dp"
            android:alpha="0.75"
            android:contentDescription="Recipient device type" />

        <TextView
            android:id="@+id/tv_msg_to"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:fontFamily="monospace"
            android:textColor="@color/text_secondary" />

    </LinearLayout>

    <!-- Message body — hidden for CFG: command messages -->
    <TextView
        android:id="@+id/tv_msg_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textColor="@color/text_primary"
        android:lineSpacingMultiplier="1.3"
        android:layout_marginBottom="6dp" />

    <!-- Command icon — shown instead of body text for CFG: messages -->
    <ImageView
        android:id="@+id/img_msg_command"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/command"
        android:scaleType="fitCenter"
        android:contentDescription="Command message"
        android:layout_marginBottom="6dp"
        android:visibility="gone" />

    <!-- Footer: status icon + status + delivery on left, priority badge on right -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="6dp">

        <ImageView
            android:id="@+id/img_msg_status"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginEnd="6dp"
            android:contentDescription="Message status" />

        <TextView
            android:id="@+id/tv_msg_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="10sp"
            android:fontFamily="monospace"
            android:textColor="@color/accent"
            android:letterSpacing="0.06"
            android:layout_marginEnd="10dp" />

        <TextView
            android:id="@+id/tv_msg_delivery"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="10sp"
            android:fontFamily="monospace"
            android:textColor="@color/text_secondary" />

        <!-- Reply button — only shown when recipient is self -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_msg_reply"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="28dp"
            android:minHeight="0dp"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="0dp"
            android:paddingBottom="0dp"
            android:text="Reply"
            android:textSize="10sp"
            android:textColor="@color/accent"
            android:layout_marginStart="8dp"
            android:visibility="gone"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            app:strokeColor="@color/accent" />

        <!-- Spacer pushes priority to far right -->
        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <!-- Priority badge — far right of footer row -->
        <TextView
            android:id="@+id/tv_msg_priority"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="10sp"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:fontFamily="monospace"
            android:paddingStart="7dp"
            android:paddingEnd="7dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:visibility="gone" />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/divider" />

</LinearLayout>
